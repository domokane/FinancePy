# Unit Test Suite

Unit tests are not intended to capture model output as this can change slightly over time. Golden tests does that

Unit tests detect changes in core deterministic outputs such as calendars, and low lying libraries 

If models are tested they are tested on edge cases that are model independent.
